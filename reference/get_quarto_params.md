# Return the YAML params during an interative session

Return the YAML params during an interative session

## Usage

``` r
get_quarto_params()
```

## Value

a list of all YAML parameters

## Examples

``` r
if (FALSE)  #dont run because function only works in interactive quarto docs

params <- get_quarto_params()
 # \dontrun{}
```
